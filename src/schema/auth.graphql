query auth($email: String!, $password: String!) {
  auth(email: $email, pwd: $password) {
    user {
      id
      name
      email
      phone
      password
      avatar
      checked
      createdAt
      updatedAt
      bio
      domain
      wechatOpenid
    }
    token
  }
}
