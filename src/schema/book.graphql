query book($id: Int!, $pagination: PaginationLegacy!) {
    book(id: $id, pagination: $pagination) {
        doubanId
        clippings {
            id
            bookID
            title
            content
            createdAt
            pageAt
        }
    }
}
