query fetchClipping($id: Int!) {
  clipping(id: $id) {
    id
    bookID
    title
    content
    createdAt
    pageAt
    visible
    comments {
      id
      content
      creator {
        id
        avatar
        name
      }
    }
  }
}
