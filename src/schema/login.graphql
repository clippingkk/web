query githubLogin($code: String!) {
  githubAuth(code: $code) {
    user {
      id
      name
      email
      password
      avatar
      checked
      createdAt
      updatedAt
      wechatOpenid
    }
    token
  }
}
