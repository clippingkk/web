/**
 * Canonical glass-surface class string. Kept for backward compatibility
 * with existing imports; new code should use the `Surface` primitive from
 * `src/components/ui/surface/surface.tsx`.
 */
export const glassCardClass =
  'rounded-2xl border border-white/40 bg-white/70 p-6 shadow-sm backdrop-blur-xl dark:border-slate-800/40 dark:bg-slate-900/70'
